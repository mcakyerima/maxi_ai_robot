# ============================================================
# MAXI AI ROBOT v2 — cloud (Railway) dependencies
# Stack: Flask + Socket.IO gateway · Groq LLM · Edge-TTS ·
#        browser STT (on the tablet) · Raspberry Pi hands over HTTP.
# ============================================================

# --- Web framework & realtime gateway ---
Flask==2.3.2
Flask-SocketIO==5.3.4
python-socketio==5.8.0
python-engineio==4.12.2
simple-websocket==1.1.0
Werkzeug==3.1.3
gunicorn==21.2.0
Jinja2==3.1.6
MarkupSafe==2.1.5

# --- LLM (Groq, free tier) ---
groq==0.25.0

# --- Text-to-speech (Microsoft Edge-TTS, free) ---
edge-tts==7.2.7

# --- HTTP clients (Pi hands, weather) ---
aiohttp==3.11.18
requests==2.32.3
certifi==2025.1.31
charset-normalizer==3.4.1
idna==3.10
urllib3==2.4.0

# --- Config & utilities ---
python-dotenv==1.1.0
typing_extensions==4.13.2
colorama==0.4.6
python-dateutil==2.9.0.post0
pytz==2025.2

# ============================================================
# OPTIONAL — advanced embedding-based memory
# brain/context_manager/ is NOT wired by default (the running system uses a
# fast in-process window memory). Removing the heavy ML stack keeps the cloud
# image small and boot fast. To enable semantic long-term memory, install:
#   sentence-transformers==4.1.0
#   transformers==4.50.3
#   torch==2.7.0
#   tiktoken==0.9.0
#   numpy==1.26.4
# then swap maxi.services.memory.WindowMemory for the context-manager backend.
# ============================================================
